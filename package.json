{
  "name": "cyclemetry",
  "version": "0.1.2",
  "scripts": {
    "dev": "CYCLEMETRY_BUILD_STAMP=$(date +%s) app/node_modules/.bin/tauri dev",
    "build": "CYCLEMETRY_BUILD_STAMP=$(date +%s) app/node_modules/.bin/tauri build",
    "lint": "pnpm --dir app lint",
    "format": "pnpm --dir app format",
    "release": "pnpm --dir app release"
  },
  "packageManager": "pnpm@10.25.0"
}
