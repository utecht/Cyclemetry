---
title: Install on Linux
description: How to download and install Cyclemetry on Linux.
---

import { Callout } from 'nextra/components'

# Install on Linux

<Callout type="info">
  Linux packages are distributed as release artifacts. Download the package that matches your architecture.
</Callout>

<p>
  <a className="install-download-button install-download-button-inline" href="https://github.com/walkersutton/cyclemetry/releases">
    <span className="install-download-icon-shell" aria-hidden="true">
      <svg className="install-download-icon" viewBox="0 0 24 24" fill="none">
        <path d="M6.5 3.75h7.25L18.5 8.5v11.75H6.5V3.75Z" stroke="currentColor" strokeWidth="1.8" strokeLinejoin="round" />
        <path d="M13.5 4v5h5" stroke="currentColor" strokeWidth="1.8" strokeLinejoin="round" />
        <path d="M9 14.75h6M9 17.25h4" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" />
      </svg>
    </span>
    <span className="install-download-copy">
      <span className="install-download-title">Download for Linux</span>
      <span className="install-download-subtitle">GitHub Releases</span>
    </span>
    <svg className="install-download-arrow" viewBox="0 0 20 20" fill="none" aria-hidden="true">
      <path d="M4.5 10h10m0 0-4-4m4 4-4 4" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" />
    </svg>
  </a>
</p>

## Install

1. Download the Linux artifact from [GitHub Releases](https://github.com/walkersutton/cyclemetry/releases).
2. Unzip the artifact.
3. Install the Debian package:

```sh
sudo apt install ./deb/cyclemetry_*_arm64.deb
```

Use the `.deb` file that matches your system architecture if the artifact includes multiple packages.

## Build From Source

If you prefer to build Cyclemetry yourself, it is a [Tauri](https://tauri.app/) app and can be built on Linux with the Tauri prerequisites installed.

### Prerequisites

Install the [Tauri Linux dependencies](https://tauri.app/start/prerequisites/#linux) for your distribution. For Ubuntu/Debian:

```sh
sudo apt update
sudo apt install libwebkit2gtk-4.1-dev \
  build-essential \
  curl \
  wget \
  file \
  libxdo-dev \
  libssl-dev \
  libayatana-appindicator3-dev \
  librsvg2-dev
```

You'll also need:

- [Node.js](https://nodejs.org/) (v20+)
- [pnpm](https://pnpm.io/)
- [Rust](https://www.rust-lang.org/) via `rustup`
- [FFmpeg](https://ffmpeg.org/) on your `PATH`

### Build

```sh
git clone https://github.com/walkersutton/cyclemetry.git
cd cyclemetry
pnpm install
pnpm build
```

The built AppImage or `.deb` will appear in `src-tauri/target/release/bundle/`.

## Ubuntu Troubleshooting

### "Apple ProRes decoder" codecs are required to play this video

If Ubuntu's built-in Video Player cannot preview a `.mov` generated by Cyclemetry and asks for Apple ProRes decoder codecs, install the restricted media packages:

```sh
sudo apt install ubuntu-restricted-extras libavcodec-extra
```

After installing those packages, the generated `.mov` should preview in the built-in Video Player application.

## Stay Updated

- ⭐ [Star the repo](https://github.com/walkersutton/cyclemetry) to get notified of releases
- 👀 Watch → Releases only to get email notifications on new releases
