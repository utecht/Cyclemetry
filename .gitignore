.DS_Store
.gstack/
.claude/worktrees/
.vscode
.tool-versions
# Build artifacts
node_modules/
__pycache__/
target/
src-tauri/binaries/
app/dist/

# FFmpeg binary (downloaded in CI — not committed)
resources/ffmpeg
resources/ffmpeg.exe

# Local secrets (Strava client secret; CI injects from Actions secrets)
src-tauri/.env

# npm lockfiles (pnpm is the package manager; demo/ tooling installs ad hoc)
app/package-lock.json
demo/package-lock.json

# App specifics
next/
*.mov
*.prof
.ruff_cache
.pytest_cache/
app/public/preview*.png
tauri_build.log

# Web companion build artifacts (synced by scripts/sync-wasm-web.mjs)
website/public/wasm/
website/public/create-demo/
